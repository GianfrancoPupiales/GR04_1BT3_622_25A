package model.utils;

import model.enums.ProductState;

import java.util.Optional;

public class ProductStateHelper {
    public static boolean isAllOrNull(Object o) {
        return false;
    }

    public static Optional<ProductState> parseState(String inputState) {
        return Optional.empty();
    }
}
